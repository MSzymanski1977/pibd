#include <iostream>
#include <stdio.h>
#include <cstring>
#include <stdlib.h>
using namespace std;

int main()
{
   size_t liczba_testow,i,j,tab[5][5];
   size_t tab_roz[7][7],k,l;
   char wiersz[5],znak;
   //do
    scanf("%d",&liczba_testow);
    scanf("%c",&znak);
   //while (cin.fail()==true);
   for(i=0;i<5;i++)
   {
       scanf("%s",wiersz);
       //printf("%s\n",wiersz);
       for(j=0;j<5;j++)
       {
           tab[i][j]=wiersz[j]-'0';
           printf("%d",tab[i][j]);
       }
   printf("\n");
   }
   for(k=1;k<6;k++)
   {
       for(l=1;l<6;l++)
       {
           tab_roz[k][l]=tab[k-1][l-1];
       }
   }
   for (i=0;i<5;i++)
   {
       tab_roz[0][i+1]=tab[4][i];
       tab_roz[6][i+1]=tab[0][i];
       tab_roz[i+1][0]=tab[i][4];
       tab_roz[i+1][6]=tab[i][0];
       tab_roz[0][0]=tab[4][4];
       tab_roz[6][6]=tab[0][0];
       tab_roz[6][0]=tab[0][4];
       tab_roz[0][6]=tab[4][0];


   }

for(i=0;i<7;i++)
   {

       //printf("%s\n",wiersz);
       for(j=0;j<7;j++)
       {
           printf("%d",tab_roz[i][j]);
       }
   printf("\n");
   }




}
